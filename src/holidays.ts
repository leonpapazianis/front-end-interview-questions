// Given a list of holidays ex: const holidays = ['10/10/2020', '01/01/2021'], write a function that accepts a list of holidays and a date object. The function should check if the given date is a holiday.


export function isHoliday(holidays: string[], date: Date): boolean {
}